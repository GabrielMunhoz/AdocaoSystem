import { FiltroAdotadosPipe } from './filtro-adotados.pipe';

describe('FiltroAdotadosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroAdotadosPipe();
    expect(pipe).toBeTruthy();
  });
});
